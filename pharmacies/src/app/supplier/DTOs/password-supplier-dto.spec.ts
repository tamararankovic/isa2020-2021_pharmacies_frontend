import { PasswordSupplierDTO } from './password-supplier-dto';

describe('PasswordSupplierDTO', () => {
  it('should create an instance', () => {
    expect(new PasswordSupplierDTO()).toBeTruthy();
  });
});
