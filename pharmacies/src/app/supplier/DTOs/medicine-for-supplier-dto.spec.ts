import { MedicineForSupplierDTO } from './medicine-for-supplier-dto';

describe('MedicineForSupplierDTO', () => {
  it('should create an instance', () => {
    expect(new MedicineForSupplierDTO()).toBeTruthy();
  });
});
