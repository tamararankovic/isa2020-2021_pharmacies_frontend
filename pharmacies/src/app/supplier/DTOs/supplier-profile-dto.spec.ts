import { SupplierProfileDTO } from './supplier-profile-dto';

describe('SupplierProfileDTO', () => {
  it('should create an instance', () => {
    expect(new SupplierProfileDTO()).toBeTruthy();
  });
});
