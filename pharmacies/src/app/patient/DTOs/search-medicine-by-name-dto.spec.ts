import { SearchMedicineByNameDTO } from './search-medicine-by-name-dto';

describe('SearchMedicineByNameDTO', () => {
  it('should create an instance', () => {
    expect(new SearchMedicineByNameDTO()).toBeTruthy();
  });
});
