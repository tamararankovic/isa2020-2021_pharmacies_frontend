import { ShowAppointmentDto } from './show-appointment-dto';

describe('ShowAppointmentDto', () => {
  it('should create an instance', () => {
    expect(new ShowAppointmentDto()).toBeTruthy();
  });
});
