import { PhamracyForMedSearchDTO } from './phamracy-for-med-search-dto';

describe('PhamracyForMedSearchDTO', () => {
  it('should create an instance', () => {
    expect(new PhamracyForMedSearchDTO()).toBeTruthy();
  });
});
