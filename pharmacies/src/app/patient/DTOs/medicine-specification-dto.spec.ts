import { MedicineSpecificationDTO } from './medicine-specification-dto';

describe('MedicineSpecificationDTO', () => {
  it('should create an instance', () => {
    expect(new MedicineSpecificationDTO()).toBeTruthy();
  });
});
