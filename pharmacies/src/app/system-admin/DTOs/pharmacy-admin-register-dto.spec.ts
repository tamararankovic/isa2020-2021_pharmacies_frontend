import { PharmacyAdminRegisterDTO } from './pharmacy-admin-register-dto';

describe('PharmacyAdminRegisterDTO', () => {
  it('should create an instance', () => {
    expect(new PharmacyAdminRegisterDTO()).toBeTruthy();
  });
});
