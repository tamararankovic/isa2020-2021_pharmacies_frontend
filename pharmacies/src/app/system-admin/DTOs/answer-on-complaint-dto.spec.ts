import { AnswerOnComplaintDTO } from './answer-on-complaint-dto';

describe('AnswerOnComplaintDTO', () => {
  it('should create an instance', () => {
    expect(new AnswerOnComplaintDTO()).toBeTruthy();
  });
});
