import { PharmacyAddAdminDTO } from './pharmacy-add-admin-dto';

describe('PharmacyAddAdminDTO', () => {
  it('should create an instance', () => {
    expect(new PharmacyAddAdminDTO()).toBeTruthy();
  });
});
