import { UserPasswordChangeDto } from './user-password-change-dto';

describe('UserPasswordChangeDto', () => {
  it('should create an instance', () => {
    expect(new UserPasswordChangeDto()).toBeTruthy();
  });
});
